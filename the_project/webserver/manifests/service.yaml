apiVersion: v1
kind: Service
metadata:
  name: webserver-svc
  namespace: project
spec:
  type: ClusterIP
  selector:
    app: webserver # This is the app as declared in the deployment.
  ports:
    - port: 3000
      protocol: TCP
      targetPort: 3001
